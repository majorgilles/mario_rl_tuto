# Data

The PyTorch Mario RL tutorial does not require a committed dataset.

- `raw/` — local raw inputs, if any.
- `processed/` — local processed files, if any.
- `external/` — third-party files that should not be committed without checking licenses.

These folders are ignored by default except for `.gitkeep` placeholders.
